"use client"

import { Box } from "@mui/material";
import MapCanvas from "./components/canvas-map";

const MapBuilder = () => {
  return (
    <Box>
      <MapCanvas />
    </Box>
  );
};

export default MapBuilder;
